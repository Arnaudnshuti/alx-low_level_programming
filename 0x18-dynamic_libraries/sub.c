#include <stdlib.h>
#include <stdio.h>

/**
 * sub - substract  two integers
 * @a: first int
 * @b: second int
 * Return: substraction after final result
 */
int sub(int a, int b)
{
	return (a - b);
}
